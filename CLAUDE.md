# WebRefurbMenu

Use `AGENTS.md` as the project instruction source. Critical fallback rules:

- Japan only; ramen and izakaya only; fixed packages are JPY 30,000 online delivery, JPY 45,000 printed + delivered, and JPY 65,000 QR menu system.
- No HVAC references.
- Lead semantics are binary: `lead: true|false`, never "maybe".
- Approved outreach routes are email and contact forms only. Phone, LINE, Instagram, and walk-ins are reference-only and never make a lead launch-ready.
- Customer-facing copy never mentions AI, automation, or internal tools.
- Preview is illustrative only; production uses owner's photos.
- Follow `PLAN.md` phase gates for product hardening work.

Startup context: read `HANDOFF.md` after project rules. Open long plans, raw lead files, and generated reports only for the current task. Keep future handoffs to roughly 40 lines by replacing stale facts instead of appending logs, search counts, or command output.
