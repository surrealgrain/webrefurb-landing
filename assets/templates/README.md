# Template Notes

- `docs/demo/ramen.html` and `docs/demo/sushi.html` are the canonical public demo pages.
- `_demo_prototype.html` and `_demo_prototype_sushi.html` are retained as archived prototype references for local asset-generation scripts only.
- `customer_qr_menu.html` is used by the dashboard preview and publish flow.
- `qr_code_sign.html` is used for printable QR sign generation.
- `demo_flow*.gif` files are demo recordings, not runtime assets.
